<article{{ attributes.addClass('mzp-c-article') }}>
  {% if title %}
  <h{{ heading_level|default('1') }} class="mzp-c-article-title">{{ title }}</h{{ heading_level|default('1') }}>
  {% endif %}
  {{ content }}
</article>
