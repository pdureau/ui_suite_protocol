{#
/**
 * @file
 * UI Pattern variant meta information.
 */
#}

{% if variant is not empty %}
<div>
  <h3 class="mzp-u-title-sm">{{ variant.label }} (<code>{{ variant.name }}</code>)</h3>
  {{ variant.description }}
</div>
{% endif %}
